[phases.setup]
nixPkgs = ["python3"]

[phases.install]
cmds = ["python3 -m ensurepip --upgrade && python3 -m pip install -r requirements.txt"]

[start]
cmd = "python3 -m gunicorn server:app --bind 0.0.0.0:$PORT"
