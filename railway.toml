[build]
builder = "nixpacks"
buildCommand = "python3 -m pip install -r requirements.txt"

[deploy]
startCommand = "python3 -m gunicorn server:app --bind 0.0.0.0:$PORT"
restartPolicyType = "on_failure"

